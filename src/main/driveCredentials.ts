/**
 * Google Drive OAuth credentials.
 *
 * Committed as an empty stub. Real values are injected by the release
 * workflow from DRIVE_CLIENT_ID / DRIVE_CLIENT_SECRET secrets, and set
 * locally for development (never commit real values).
 */
export const DRIVE_CREDENTIALS = {
  clientId: '',
  clientSecret: '',
  scopes: [],
  redirectPort: 0,
};
